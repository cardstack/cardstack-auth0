{{#cardstack-session as |session|}}
  {{#if session.isAuthenticated }}
    <img src="{{session.user.avatarUrl}}">
    <button {{action session.logout}}>Logout {{session.user.name}}</button>
  {{else}}
    {{#auth0-login as |login|}}
      <button {{action login}}>Login</button>
    {{/auth0-login}}
  {{/if}}
{{/cardstack-session}}
